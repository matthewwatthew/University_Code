public class RecursiveBinarySearch {
	public static int binarySearch(int[] arr, int value){
		int first = arr[0];
		int last = arr[-1];
		if(arr[0] == value){
			return 1;
		}
		int x = arr.length/2 {
			return binarySearch(arr, start + 1)
		}
		return value;
	}
	public static int binarySearch(int[]arr) {
		return binarySearch(arr, 0);
	}
}


// Would keep track of the first and the last and recurse until the first and last are equal to each other.
// return the middle if the value is correct.
// else recurse again. 